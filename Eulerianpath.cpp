#include <bits/stdc++.h>
using namespace std;

struct Eulerianpath {
    vector<vector<int>> g;

    Eulerianpath(int n) {
        g.resize(n);
    }

    void getpath(int s) {
        
    }
};

signed main() {}